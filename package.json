{
  "name": "pratiquez",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "pratiquez-install": "npm install client/ && npm install server/ && npm install",
    "dev": "concurrently 'cd server && npm run start-watch' 'cd client && npm start'"
  },
  "keywords": [],
  "author": "Scott Adams",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^4.1.0"
  }
}
